FUNCTION removeExistingProduct(id, products)

    FOR index = 0 TO length(products) - 1

        IF products[index].id == id THEN
            products.remove(index)
        ENDIF

    ENDFOR

ENDFUNCTION

